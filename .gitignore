build/
.gradle/
.kotlin/
local.properties
*.hprof
node_modules/

# Yarn's lock for the wasm toolchain is generated per-build here; the library does not ship a
# browser bundle, so there is nothing for it to pin.
kotlin-js-store/
